name: build-and-test

on:
  push:
    branches-ignore:
      - "*"

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Print Hello, World
        run: echo "Hello, world"
